/* Arizona catalog — intentionally EMPTY for now.
   Arizona licensing (AZ ROC for contractors, AZ MVD for driving, AZ State Board of
   Cosmetology, etc.) differs from California, so its courses must be authored from
   Arizona's own official sources. Until then this state is selectable but shows the
   "no courses yet" empty state.

   To add an AZ course: push a category here (same shape as ca.js) + drop
   js/questions/<id>.js. Keep AZ course ids distinct from CA (e.g. az-... prefixes). */
registerState("az", []);
