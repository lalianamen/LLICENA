# Secrets — never commit. Supabase service_role keys, Stripe keys, webhook
# secrets, etc. belong in env files / platform secret stores, not the repo.
.env
.env.*
*.local

# Dependencies (if a build/tooling step is ever added)
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
